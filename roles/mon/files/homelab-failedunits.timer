[Unit]
Description=Sweep for failed systemd units every 15 minutes

[Timer]
OnBootSec=8min
OnUnitActiveSec=15min
# Boot-time sweeps from three VMs would otherwise all land on ntfy at once.
RandomizedDelaySec=60
Persistent=true

[Install]
WantedBy=timers.target
